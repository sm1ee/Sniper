# CodeMirror 6 Bundle for Sniper

One-time build. The output `../codemirror.bundle.js` is committed.

## Rebuild

```bash
cd web/vendor
npm install
npm run build
```

## Packages

See package.json for exact versions.
